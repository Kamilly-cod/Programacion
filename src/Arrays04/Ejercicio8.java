package Arrays04;

import java.util.Random;
import java.util.Scanner;

/*
Realizar un programa que pida un tamaño de array por tgeclado y a continuación cree un array formado por ele,mentos aleatorios pares entre 1 y 50.

public class Ejercicio8 {
    public static void main(String[] args) {
        Scanner teclado = new Scanner(System.in);
        int tamaño = teclado.nextInt();

        int [] array = new int[];
        for (int i = 0; i < tamaño ; i++) {
            Random generador = new Random();
            int numero = generador.nextInt(1,51);

            if ( numero % 2 == 0) {
                array[i] = numero;
            }

        }

    }
}
 */